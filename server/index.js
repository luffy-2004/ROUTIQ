// server proxy removed — external API integration rolled back.
// Keep file to avoid missing-file errors if scripts reference it; it does nothing.

console.log('Server proxy disabled (removed by rollback).');
